## Cleanup files that landed in the wrong place
for dir in Ext Mod; do
	[[ -d %{buildroot}/usr/${dir} ]] || continue
	pushd %{buildroot}/usr/${dir}
	mkdir -p %{buildroot}/usr/share/freecad/data/${dir}/
	cp -pr * %{buildroot}/usr/share/freecad/data/${dir}/
	popd
	rm -rf %{buildroot}/usr/${dir}
done
